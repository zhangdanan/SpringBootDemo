<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sloth.zhang.shiro.mapper.UserMapper">

    <select id="queryUserByName" resultType="sloth.zhang.shiro.pojo.User">
        select * from shiro where userName=#{userName}
    </select>

</mapper>